# Release signing config. Copy this file to keystore.properties and fill in the values.
# NEVER commit keystore.properties or .jks files.
#
# Generate a keystore:
#   keytool -genkeypair -v -keystore fling-release.jks -keyalg RSA -keysize 2048 -validity 10000 -alias fling

storeFile=../fling-release.jks
storePassword=
keyAlias=fling
keyPassword=
